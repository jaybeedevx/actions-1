name: Generate Art work with ASCII

on : push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/Checkout@v5

        - name: List Repo Files
          run: ls -ltra

        - name: Executing Shell Script
          run: ./asscii-script.sh
